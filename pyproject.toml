[project]
name = "wechat-video-download"
version = "0.2.0"
description = "Email bot that downloads Douyin videos from shared links"
requires-python = ">=3.10"
dependencies = [
    "f2>=0.0.1.7",
    "playwright>=1.60.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
]
